The Nexora console is now available at your selected domain/path.

An API token has been generated and stored in the app data directory. You will need this token to access the API programmatically. Check the admin panel or the file at `__DATA_DIR__/api-token`.

By default, Nexora starts in **observer** mode — read-only, no destructive actions. Change the profile in `config.toml` to enable additional capabilities.
