Choose a free path such as `/nexora`. On existing populated YunoHost nodes, adoption mode is recommended — it scans the current state, detects path collisions, and proposes a safe integration plan before making any changes.

Nexora requires approximately 300MB of RAM and 250MB of disk space. Docker support requires Docker Engine to be installed separately.
